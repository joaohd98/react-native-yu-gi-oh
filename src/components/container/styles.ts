import styled from "styled-components/native";
import {SafeAreaViewProps} from "react-native-safe-area-context";

export const ContainerStyles = {
  SafeAreaView: styled.SafeAreaView<SafeAreaViewProps>`
    flex: 1;
  `,
};
