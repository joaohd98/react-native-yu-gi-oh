import styled from "styled-components/native";
import {Colors} from "../../theme/colors";

export const CustomTextStyles = {
  Container: styled.View`
    background-color: ${Colors.skeletonColorText};
  `,
};
